import React from 'react';
import Header from './Header';

const NotFound = () => {
  return (
    <React.Fragment>
      <Header />
      <div className='container-fluid'>Not found</div>
    </React.Fragment>
  );
};

export default NotFound;
