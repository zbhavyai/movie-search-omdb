import React from 'react';
import Header from './Header';
import SearchBar from './SearchBar';

const Home = () => {
  return (
    <React.Fragment>
      <Header />
      <SearchBar />
    </React.Fragment>
  );
};

export default Home;
